"""
Train similarity computed from spike times
"""
# function TrainSimilarity(spikes, # Can be vectors of times or matrix of rates
#                          max_lag::Number;
#                          time_windows = nothing, # For declaring intervals over which to compare
#                          fold_proportion::Float64 = 0.1,
#                          inclusive_edge = :right,
#                          num_perms::Int = 100)

    

# end
